# Scene Engine

## Purpose

The Scene Engine manages scene configurations without executing them (execution remains in Mobile Core Engine).

## API Endpoints

- `POST /` — Create a scene
- `GET /` — List scenes
- `GET /:id` — Get scene by ID
- `PATCH /:id` — Update scene attributes
- `DELETE /:id` — Delete a scene
