# Device Health Engine

## Purpose

The Device Health Engine manages incoming heartbeat telemetry reports.

## API Endpoints

- `POST /:deviceId/heartbeat` — Submit device heartbeat report
- `GET /:deviceId/health` — Get health summary of a device
