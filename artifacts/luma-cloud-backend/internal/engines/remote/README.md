# Remote Access Engine

## Purpose

The Remote Access Engine enables secure remote control via MQTT publish-subscribe conventions.

## API Endpoints

- `POST /:deviceId/commands` — Send a remote control command to a device
