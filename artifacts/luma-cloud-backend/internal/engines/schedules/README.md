# Schedule Engine

## Purpose

The Schedule Engine manages schedule configurations without executing them (execution remains in Mobile Core Engine).

## API Endpoints

- `POST /` — Create a schedule
- `GET /` — List schedules
- `GET /:id` — Get schedule by ID
- `PATCH /:id` — Update schedule attributes
- `DELETE /:id` — Delete a schedule
